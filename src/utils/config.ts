const config = {
  serverApi: 'http://127.0.0.1:4000',
  jwtToken: 'jwtToken',
  shouldUseFakeActiveProgram: true,
  shouldUseFakeHistory: true,
  fakePasswordRegex: true,
  shouldShowLoginScreen: false
}

export default config
