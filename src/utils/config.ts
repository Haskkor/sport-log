const config = {
  serverApi: 'http://127.0.0.1:4000',
  jwtToken: 'jwtToken',
  shouldUseFakeActiveProgram: false,
  shouldUseFakeHistory: false,
  fakePasswordRegex: true,
  shouldShowLoginScreen: false
}

export default config
